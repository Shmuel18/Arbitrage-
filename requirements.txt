# Trinity Arbitrage Engine v3.0

# Exchange connectivity
ccxt>=4.2

# Async
aiohttp>=3.9
# Fast asyncio event loop (libuv-based) — Linux/Mac only, no-op on Windows
uvloop>=0.19; sys_platform != "win32"

# API server
fastapi>=0.110
uvicorn[standard]>=0.27

# AI assistant — any/all providers (auto-fallback on quota)
anthropic>=0.40                 # Paid, best tool use
google-generativeai>=0.8         # Free tier: 1500 req/day (2.0-flash)
groq>=0.11                       # Free tier: 14,400 req/day (llama-3.3-70b)

# Storage
redis[hiredis]>=5.0

# Config & validation
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0

# Testing
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
